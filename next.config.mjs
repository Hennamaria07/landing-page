/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['plus.unsplash.com', 'static.vecteezy.com', 'www.heysaturday.co', 'www.mensjournal.com', 'www.shutterstock.com', 'clubcorvettemontreal.com', 'media.istockphoto.com', 'media.istockphoto.com', 's3-alpha-sig.figma.com']
    }
};

export default nextConfig;
